{
  "mcpServers": {
    "codex-keyring": {
      "type": "stdio",
      "command": "codex-keyring",
      "args": [
        "mcp"
      ],
      "note": "Local stdio MCP server for codex-keyring. Exposes multi-account alias management, manual switching, auto-switch health checks, and status tools to Codex app, CLI, and IDE extension."
    }
  }
}
